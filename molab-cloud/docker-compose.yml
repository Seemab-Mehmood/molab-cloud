version: "3.8"
services:
  molab-cloud:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - molab-data:/app/data
    restart: unless-stopped

volumes:
  molab-data:
